package com.fur.enumerate;

public enum OperatorList {
    SUFFIXINC, SUFFIXDEC,
    POS, NEG, PREFIXINC, PREFIXDEC,
    NOT, LOGICALNOT,
    MUL, DIV, MOD,
    ADD, SUB,
    LEFTSHIFT, RIGHTSHIFT,
    LEQ, GEQ, LT, GT,
    EQUAL, NOTEQUAL,
    AND,
    XOR,
    OR,
    LOGICALAND,
    LOGICALOR,
    ASSIGN,

    ALLOC,
    RETURN
}
